<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <!-- Button -->
    <style name="ButtonTheme" >
        <item name="android:layout_width">fill_parent</item>
        <item name="android:layout_height">@dimen/DIMEN_48PX</item>
        <item name="android:layout_marginLeft">@dimen/DIMEN_18PX</item>
        <item name="android:layout_marginRight">@dimen/DIMEN_18PX</item>
        <item name="android:layout_alignParentBottom">true</item>
        <item name="android:layout_marginBottom">@dimen/DIMEN_18PX</item>
        <item name="android:background">@drawable/bg_button_selector</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/TEXT_SIZE_14SP</item>
    </style>

    <!-- Tab Bottom -->
    <style name="main_tab_bottom">
        <item name="android:textSize">@dimen/DIMEN_12PX</item>
        <item name="android:textColor">@color/icon_text_grey</item>
        <item name="android:ellipsize">marquee</item>
        <item name="android:gravity">center_horizontal</item>
        <item name="android:paddingTop">@dimen/DIMEN_2PX</item>
        <item name="android:layout_width">fill_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:button">@null</item>
        <item name="android:singleLine">true</item>
        <item name="android:drawablePadding">@dimen/DIMEN_2PX</item>
        <item name="android:layout_weight">1.0</item>
    </style>

</resources>
